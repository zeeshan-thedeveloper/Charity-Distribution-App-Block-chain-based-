class Organization {
  String title, email, description, blockchainAccountAddress, userUid;
  Organization(
      {required this.title,
      required this.email,
      required this.userUid,
      required this.description,
      required this.blockchainAccountAddress});
}
